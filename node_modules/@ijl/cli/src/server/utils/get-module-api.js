const path = require('path');
const watchApi = require('./watch-project-api');

module.exports = (app, location) => {
    // app.use('/api', require(path.resolve(location)))
    watchApi(app, path.resolve(location))
}
