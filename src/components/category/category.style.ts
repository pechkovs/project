import styled from '@emotion/styled'
import Container from '../../components/container/container'

export const CategoryPage = styled(Container)`
    z-index: 20;
    position: static;
    background: #ffffffe0;
    text-align: center;
    color: #bf9270;
    margin: auto;
    font-size: 2em;
    font-family: 'Cormorant Unicase', serif;
`
