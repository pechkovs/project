import styled from '@emotion/styled'

export const stSummary = styled.div`
    margin: 2rem 0;
`
export const stSummary__box = styled.div`
    @include flex(center, space-between);
    flex-wrap: wrap;
    margin-top: 1rem;
    margin-right: -1rem;
    margin-left: -1rem;
`
